# Local mini-ensemble (2026-09-15) - blend-weight calibration, NOT a leaderboard run

Six folds of `configs/config_ci_ensemble.yaml` trained on `data/fixture/` (the real
512x512 competition window at row 2048 / col 1280), 20 epochs, resnet34,
`pretrained: false` (weights host unreachable from this sandbox). Purpose: exercise the
exact fold -> raw-map -> blend -> shaping path of the parallel workflow and MEASURE the
fold-weighting choice. Fold best held-out shaped DTIs: 0.1497, 0.0488, 0.1919, 0.2610,
0.0266, 0.1625 (toy numbers - 24-48 windows each, undertrained models).

Informational DTI of the FINAL shaped blend vs the PUBLIC known faults (blanket-ones
floor 0.0955 for this window; wrong-universe metric, used here only to compare the two
weighting rules on identical maps):

| folds blended | equal weights | softmax-over-heldout-DTI weights | verdict |
|---|---:|---:|---|
| 2 (folds 0-1) | 0.0143 | **0.0984** | weighting rescues a strong fold from a weak one |
| 6 | **0.1033** | 0.0656 | weighting over-concentrates on crop-noisy heldout DTI |

**Decision (recorded in the blend CLI help + the workflow):** equal averaging is the
default for the 6-fold CI ensemble; `--weights dti` stays available for excluding a
known-catastrophic fold. This is exactly the kind of thing not to decide by taste.

All four reports are machine-generated by `scripts/blend_submission.py`:
`blend_report_equal_weights.json` (2-fold, equal), `blend_report_dti_weights.json`
(2-fold, weighted; also `blend_report.json` = same, picked up by the site build),
`blend6_report_equal.json` and `blend6_report_dti_weights.json` (6-fold).
