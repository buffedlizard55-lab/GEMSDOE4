numpy>=1.24
rasterio>=1.3
scikit-image>=0.21
scikit-learn>=1.3
segmentation-models-pytorch>=0.3
torch>=2.0
torchvision>=0.16
matplotlib>=3.7
tqdm>=4.65
pyyaml>=6.0
scipy>=1.10
geopandas>=0.13
pandas>=2.0
einops>=0.6            # pulled in by smp's transformer decoders (SegFormer)

# NOTE (2026-09-12): albumentations was dropped - src/dataset.py does its own
# label-consistent numpy augmentation (crop/flips/rot90/noise) so no extra dep is needed.
# Exact versions verified installable + importable: see requirements.verified.txt
